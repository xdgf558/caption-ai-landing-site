# R1 复审修复（2026-09-21）

基线：网站 #179 `9c1a319`、iOS #9 `f911531`。没有生产部署、开关变更或自动压缩。

## 回执兼容范围

`libraryReceipts` 使用与请求入口相同的 `validID()`（16–128 个 ASCII 字母、数字、下划线或连字符），同时约束旧 JSON 和紧凑格式读取。不能通过只收紧新写入来修复已持久化的合法回执。

实际 Miniflare/D1 回归覆盖 16、36、80、81、128 字符取消收藏：首次提交、原请求重复三次、隔离压缩后重复三次、还原后重复三次。原回执、请求摘要、唯一操作数、资源版本保持一致，还原后原始行逐项一致。81/128 场景在修复前失败（首次 200，重放 503），修复后通过。

## A11 准备阶段与失败证据

保留旧 push `35547800516` 的失败：宿主 PID 6409 在 `seedRequest` 报 `url_error/-1001`，没有到达崩溃边界。原 artifact ZIP SHA256 为 `520343402df3563d932f0735ab4cb3f78452886949e3d18baea2c5f9b2318a13`。旧 `seed_password` 合并了同步 PBKDF2 与 D1 写入，共 28.012 秒；不能从旧记录判断是哪一项慢，也不能据此推断 HTTP 500 或产品认证回归。

测试桥新增受随机 proof 保护的 POST `/fixture/prepare`，由宿主在启动 CRASH App 前调用一次，执行原有真实密码、授权码和 token 流程。GET `/fixture/seed?stage=…` 只读取内存中已准备的同阶段会话。未准备、错误阶段、重复准备及并发准备均拒绝；失败准备不允许在本进程中重新执行该阶段。准备响应只包含阶段与 ready，凭据仍只通过受保护的本机读取交给探针。

iOS 宿主给完整准备请求一个独立的 90 秒绝对上限（含连接、响应头、响应体），到期关闭连接并终止验证，不重试 POST，不重跑恢复。此上限发生在第一次 refresh 之前，服务端 120 秒重放期限、5 分钟认证期限及 App refresh 超时均未放宽。密码计算和写入各自记录固定标签的开始、结束或错误类别，不输出凭据。

新增真实 Worker/D1 桥测试检查单次准备、并发拒绝、重复/错误阶段拒绝、读取稳定性及 refresh 尚未发生；诊断测试继续检查脱敏和预算。实际模拟器验证与新 head CI 记录见 iOS 配套复审报告。原失败被保留，不能因另一条 PR CI 成功而将其标记为已解释的环境波动。

本次本机结果：资料库 23/23、生命周期与编解码 12/12、认证 29/29、准备桥与诊断 3/3；Astro 验证构建通过（ALLOW_EMPTY_SERIAL_CONTENT=1，不能作为生产包）。
