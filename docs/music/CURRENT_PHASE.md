# 当前阶段

更新：2026-09-10。

当前状态：M0 PR #117、原会员 PR #118、人工退款审核 PR #119、音乐基础 PR #120、只读 VIP PR #121 均已合并；#118/#119 已完成授权的迁移与部署。用户报告真实兑换、退款后台登录后验收及真实撤销已人工完成，不扩大为 Safari 或多实例并发验证。本轮按授权先合并 #121 到 main@233d716，再实施 M1-04 条件发布，23 项隔离测试完成，待独立 PR 审查。见 [条件发布](M1_PUBLICATION.md)。

- 当前：PREP、M0-01 至 M0-05、M1-01 至 M1-04 已完成本地实现和测试。
- M0 DONE 仅表示核对完成；原会员修复状态以下表及 PR #118 为准，不表示音乐具备上线资格。
- M1-05、M2–M6：TODO，未启动。M1 整阶段尚未完成；没有真实资源验证器时发布仍返回 503。
- 历史 M0 规划分支：codex/music-planning；创建基线：main@3b8bbc8c324e65e721d9b1b989d6d87b988f447d。
- 原会员独立修复分支：codex/fix-membership-redemption-safety；基线：main@19a4436。
- 当前开发分支：codex/music-conditional-publication。下一步：审查本轮，再推进 MP3 实测；不自动开放音乐。涉及 UI 时必须调用 Product Design 技能。

## 待核对和上线门槛

| 事项 | 当前证据状态 | 后续处理 |
| --- | --- | --- |
| 现有 VIP 来源 | PR #118 已严格校验 UTC 起止，未来开始、空/非法日期和未知类型不授予 VIP；仍为单账号单期限，无多 grant/终身支持 | 音乐只读适配，不复制账本；续兑不修正历史未来 started_at，此类行仍失败关闭 |
| 兑换一致性 | PR #118 已用同一 D1 batch、operation_token 和 NOT NULL guard 保证回执/扣点/期限/ledger 一致；账号隔离的持久键支持重放，本地故障回归通过 | 保持现价与周期；缺迁移返回 503 且不扣点，生产多实例验收待执行 |
| 退款/拒付/撤销 | #119 已上线；用户报告后台登录后验收及真实撤销已人工完成 | 只读使用撤销后的原会员有效期，不把音乐归因表当资格；历史无回执或关联不明的记录不能自动撤销 |
| 路由、Access、回跳 | PR #118 共用 safeReturnPath；M1-03 独立只读资格模块使用单次 first-primary SELECT，capabilities 复用现有四语会员中心路径，尚未接 Worker | 音乐路由和逐字节媒体入口属 M2；音乐精确回跳、路由白名单和独立语言映射仍待 M4 |
| 原会员生产上线 | #118 的 0036、#119 的 0037 及对应 Worker/页面已部署；部署证据见各 PR 留言 | 不得回退旧兑换、清理操作回执或审计；用户人工验收不替代 Safari/多实例与故障重试专项 |
| MP3 服务端限资源测时 | 尚未选型与验证 | M1-05 单独审查 VBR/ID3/损坏文件/预算；失败不得发布受保护歌曲 |
| 音乐库、私有桶、预发、配额和账单提醒 | 独立 migrations-music 初始化在内存 SQLite 与本地 D1 模拟器通过；无生产绑定/资源 | 创建或修改真实资源需另行授权，不得把音乐迁移应用到 WAITLIST_DB |
| 正式歌曲、完整音频、试听及权利材料 | 本轮未收到/未核对 | 可用合法隔离 fixtures 开发，不可充作正式作品发布 |
| 统计隐私依据与保留策略 | 未确认 | 实现后仍默认关闭；不可用归因明确标注 |
| iPhone 等真实设备验收 | 未运行 | M6 记录设备与实测结果，模拟浏览器不代替真机 |

PR #118 已运行 npm test、npm run build，均通过；144 页、111 条 sitemap；支付 Playwright 30 例通过，包含同键重试、账号隔离、双标签互斥与恶意回跳。上线验证记录见 PR #118 部署评论；用户报告真实兑换成功，以及 #119 后台登录后验收和真实撤销完成。代理未独立执行真实 Safari、Cloudflare 多实例或真实退款测试。无 Web Locks 的浏览器会在 POST 前拒绝兑换，不降级收费。

MEMBERSHIP_INTEGRATION 与 TEST_MATRIX 中的 M0 审计仍是历史基线证据；原缺陷脚本保存在 git@19a4436，当前审计入口已执行正确行为回归。不得将历史缺陷复现当作当前修复状态，也不得用本地 fixtures 代替生产验收。

四个拟新增开关 MUSIC_PUBLIC_ENABLED、MUSIC_UPLOADS_ENABLED、MUSIC_VIP_DELIVERY_ENABLED、MUSIC_ANALYTICS_ENABLED 都按默认 false 规划；当前未写入 wrangler 或创建开关。
