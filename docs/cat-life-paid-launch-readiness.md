# 任务一：首批游戏付费内容上线准备盘点

审查日期：2026-09-05。基线：`main` / `6a2f9cb`，游戏内容版本 `1.22.0`。

本报告保留盘点时证据；后续代码修复与仍待执行的生产步骤见[任务二 A](cat-life-paid-launch-gap-fixes.md)。不要将历史待核验项自动视为通过。

## 结论

保留两项首发候选：月夜虎斑皮肤、车站小屋主题套装。先验证单皮肤，再验证主题套装；暂不增加第三个 SKU 或新游戏开发。

**盘点完成不代表可以开卖。** 当前结论是保留白名单、补齐下列售前展示与生产验收证据，再申请分阶段开放。没有修改价格、商品状态、rollout、白名单密钥或生产账户。

## 证据范围

- 读取服务端种子、内容 manifest、商城渲染、房间渲染、商品介绍页、发布手册和测试代码。
- 已检查套装宣传图；它是内容插画，不是当前房间运行截图。
- 本地测试使用 VM、测试数据库和模拟 API；不能证明生产 D1 的当前价格、商品状态或真实支付成功。
- 初次生产观察与 PR 复核均只使用不带凭证的游客目录 GET；复核的可追溯记录见下表。两次结果一致：`ok: true`、`authenticated: false`、`rollout.mode: allowlist`、`catalogVisible: false`、`redemptionEnabled: false`、`products: []`。
- 该结果只能证明游客被挡住，不能证明白名单密钥正确、非白名单会员被挡住，或两个商品都已暂停。
- 历史对话提及月夜商品测试和手动测试积分；本次未取得可对应的生产订单、账本和冲正审计证据，不追认成已完成验收，也不重复赠送或扣点。证据应保存在受限后台，不将邮箱、账户和订单明细提交到仓库。

### 生产游客目录复核记录

这是 PR 审查后的重新请求，不为初次观察补造时间或版本。

| 字段 | 实测值 |
| --- | --- |
| 请求 | `GET https://wwwstationcat.org/api/games/cat-life/catalog?locale=zh-Hans`，未发送 Cookie 或 Authorization |
| 客户端开始时间（UTC） | `2026-09-04T21:49:33.996Z` |
| 客户端完成时间（UTC） | `2026-09-04T21:49:35.406Z`（新加坡时间 2026-09-05 05:49:35.406） |
| 目标站点 HTTP 状态 | `HTTP/2 200`；不把代理的 `200 Connection established` 当作站点状态 |
| 响应 Date | `Fri, 04 Sep 2026 21:49:35 GMT` |
| 响应缓存策略 | `Cache-Control: no-store` |
| Cloudflare 请求标识 | `cf-ray: a36034731d64d183-LAX` |
| 生产部署版本 / commit | 未核实：响应未包含部署 ID 或 commit，本次未访问 Cloudflare 部署记录。本文代码基线 `6a2f9cb` 不等于已核验的线上 SHA |

后续生产验收应在受限发布记录中补上此时间窗口对应的 Worker deployment/version ID、关联 commit 和查询时间；若有灰度多版本，应保留版本分流信息，不能仅凭最新部署推断本次请求使用的版本。完成关联前，这条证据只支持当时游客目录的行为，不支持某次部署已通过验收。

## 首批商品交付边界

| 项目 | 月夜虎斑皮肤 | 车站小屋主题套装 |
| --- | --- | --- |
| 商品 ID | `cat-life.skin.moonlit-tabby` | `cat-life.bundle.station-room` |
| 权益 ID | `cat-life.cosmetic.skin.moonlit-tabby.v1` | `cat-life.content.furniture.station-room.v1` |
| 种子价格 | 10 Station Points | 25 Station Points |
| 实际交付 | `cat_001` 的银蓝虎斑/月亮项圈外观，坐姿和行走素材 | 车站绿墙、条纹地面、信号装饰、候车布局；时钟板、信号灯、长椅三件条件装饰 |
| 生效条件 | 当前账号有有效权益，并选择装备；不是给全部猫换皮肤 | 当前账号有有效权益，并在小家装修中选择对应主题项 |
| 三件装饰的出现规则 | 不适用 | 绿墙显示时钟板；信号装饰显示信号灯；候车布局显示长椅，并非购买后立即同时显示 |
| 不包含 | 新猫、属性加成、金币、工作收益、全猫适配 | 房间扩容、舒适度/收益加成、独立背包家具、任意拖拽三件装饰；宣传图的桌子/花瓶/地毯不作为独立物品交付 |
| 持有规则 | 一次性、永久、账号绑定、不可转让；官方授权由服务器保存 | 同左；不授权其他游戏 |

来源：`server/catalog/cat-life-game-commerce.v1.json`、`public/games/cat-life/content-manifest.js`、`public/games/cat-life/commerce.js`、`public/games/cat-life/src/js/systems/homeSystem.js`。

10/25 点是现有种子值，本次没有改价。生产 D1 可经 Admin 改价，正式验收须读取实际 `points_price` 和 `catalog_revision`。积分价格不等于玩家首次支付金额；应另外核对可购买积分包、最低充值额和剩余积分说明，不宣传未经验证的现金单价。

## 实现与验证状态

| 项目 | 本次确认 | 尚不能据此推断 |
| --- | --- | --- |
| 商城与价格 | 会员店独立；价格与余额取 API；确认提交仅含 productId/idempotencyKey | 生产商品价格已审核 |
| 授权与隔离 | 皮肤需权益与偏好同时成立；主题经过权限过滤；离线缓存按账号隔离 | 每台真实设备的生产同步均已实测 |
| 交易与冲正 | 存储/API/Admin 测试包含幂等、并发、伪造字段、余额不足、冲正 | 历史真实订单已经对账 |
| 预览 | 两个商品有图片；manifest 声明皮肤及三件装饰资产 | 插画等于最终房间效果，或整图所画均能独立摆放 |
| 支持规则 | 现有契约和发布手册规定购买冲正返还原积分、保留账本；不使用手动撤销代替冲正 | 游戏积分冲正等于现金支付退款 |
| 安全开关 | 仓库与生产游客响应均为 allowlist；缺省/非法模式按 off | 已允许 public，或商品已 active/paused |

## 开卖前缺口与验收要求

| 优先级 | 缺口 | 下一步与通过条件 |
| --- | --- | --- |
| 必须 | 套装插画与实际交付范围容易混淆 | 展示真实房间效果；中/繁/英/日商品说明列出四项主题、三件条件装饰、使用位置和不包含项。保留插画时明确标示为示意，不把桌子等作为交付承诺 |
| 必须 | 皮肤说明写了猫名，但未明确排除其他猫 | 明确仅适用 `cat_001` 对应猫，不承诺所有猫。已核对英文 Sunny 与 `catsData.js` 一致，不是翻译错误；若玩家改名，仍需能识别适用对象 |
| 必须 | 套装现有浏览器用例直接注入已购权益并调用渲染器 | 补完整 UI 路径：未拥有 -> 确认 25 点 -> 成功 -> 小家装修选择四项 -> 重新载入 -> 撤销后回默认；断言资产成功加载，桌面与 390px 留实际截图供审查 |
| 必须 | 两个 SKU 缺本次可核对的生产验收记录 | 按下方模板分别验收，不以皮肤成功替代套装；补非白名单会员、第二设备、离线恢复、冲正重放与暂停后的持有者访问 |
| 必须 | 首次付款与支持说明未完整走查 | 核对充值套餐/最低金额与到账，兑换确认的商品范围和规则入口；提供可找到的客服入口，区分积分冲正和支付退款；不在本次新增或承诺退款政策 |
| 上线观察前 | 没有本次确认的留存/真实付费基线 | 按任务清单定义统计口径；赠送测试积分与实际付费分组，设定观察窗口、复核人和异常处理规则后再扩大范围 |

以上是任务二的明确输入，不在本 PR 中悄悄改造 UI、增加追踪或激活商品。售前展示修改需要单独审查，并同步游戏版本说明。

## 生产验收记录模板

每个 SKU 独立建立受限记录。仅在本报告填写脱敏结论和日期；真实账号、订单、账本及截图放受限审计系统。本次以下各项均为待核验。

| 验收项 | 月夜虎斑 | 车站小屋 |
| --- | --- | --- |
| 版本/部署、rollout、Admin 当前状态、价格/目录修订已记录 | 待核验 | 待核验 |
| 游客、非白名单会员被挡住；白名单看到正确商品与价格 | 待核验 | 待核验 |
| 商品预览、确切范围、支持规则与实际效果相符 | 待核验 | 待核验 |
| 记录原余额 B，兑换后 B-P；一笔 completed、一笔扣点、一个有效权益 | 待核验 | 待核验 |
| 同键重试不重复扣点；刷新及第二设备可用，其他账号不可用 | 待核验 | 待核验 |
| 同账号离线可展示但不能购买，恢复联网后校准权益 | 待核验 | 待核验 |
| Admin 购买冲正一次返还 P；重放不再返还；撤销后同步回免费外观 | 待核验 | 待核验 |
| 暂停后不可新购；另一个合法持有者仍可使用；历史记录保留 | 待核验 | 待核验 |
| 无 pending/reversing 遗留、缺授权 completed 单或异常负余额；审核签字 | 待核验 | 待核验 |

余额检查须排除并行充值/消费，用订单和账本解释差额。不要为了满足测试任意制造生产持有者或赠送积分；已有记录可验证则复用记录，否则先取得授权。

## 本地验证记录

测试在上述基线上运行。模拟 API 的浏览器测试不连接生产会员或扣除真实积分。

- `npm test`：通过，含内容 manifest、交易存储、API、Admin、云存档和支付回归。
- `node scripts/test-cat-life-content-manifest.mjs`：通过。
- `npm run test:browser -- scripts/browser-tests/cat-life-commerce.spec.mjs`：Chromium 11/11 通过，含 390px、皮肤兑换、房间权益、伪造存档、离线与撤销。首次运行被沙箱禁止监听端口；允许本地测试服务后重跑通过。未运行 Safari/WebKit 或真实支付测试。
- `npm run build`：通过，143 页、111 条静态 sitemap URL、4 个本地化 404；静态小说内容目录为空的警告仍存在，不代表生产 D1 小说为空。
- `git diff --check`：通过。

## 后续操作边界

下一步先完成缺口修复 PR。审查通过后另行批准生产验证：维持 allowlist，逐个商品激活、验收、冲正、暂停。两项都通过后再进行有限白名单观察，最后单独审批 public 配置。

遇到异常按 [发布手册](cat-life-game-commerce-release.md) 关闭新兑换，保留账本与既有权益，不删除历史记录。未核验项不能因合并或部署自动变成通过。
