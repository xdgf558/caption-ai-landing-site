# A11 隔离验证诊断

2026-09-20：iOS #8 的 998bbfc push 35519123901 在 heldPolling 内四次受限 GET 重试后超时；PR 35519125775 完整成功。未将失败认定为环境波动。产品 Core 与 120 秒服务端期限不变。

测试服务现在将固定步骤的 start/done/failed 写到私有临时目录 diagnostics.jsonl，每条 fsync，最大 256 KiB。仅保留步骤枚举、序号、时间、固定错误分类/错误码和异常文本 SHA256；无凭据、URL、请求体、SQL 或异常原文。挂起步骤可以通过缺少 done 来定位。此文件只供诊断，不能替代真实数据库/请求断言，不能使失败探针通过。iOS 驱动在停止服务后复制文件，无需再请求 HTTP。

网站 PR #178 因此增加测试基础设施修改，不再只有政策文档；无产品 Worker、认证、迁移、支付、开关或部署修改。iOS 恢复 fixture 固定版本将跟随此提交；需重新执行真实 A11–A13 与完整稳定 CI。底层问题在捕获到明确证据前仍属未解释失败。
