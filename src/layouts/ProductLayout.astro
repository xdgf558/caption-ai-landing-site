---
import BaseLayout from './BaseLayout.astro';
import ProductNightLayout from './ProductNightLayout.astro';

interface AlternateLink {
  hrefLang: string;
  href: string;
}

interface Props {
  title: string;
  description: string;
  lang?: 'en' | 'ja' | 'zh-Hant' | 'zh-Hans';
  canonicalPath: string;
  alternates?: AlternateLink[];
  bodyClass?: string;
  ogImage?: string;
}

const {
  title,
  description,
  lang = 'zh-Hant',
  canonicalPath,
  alternates = [],
  bodyClass,
  ogImage = '/images/apps/snapcopy-app-icon.png'
} = Astro.props;
// Only the redesigned product landing opts into the night theme.
const Layout = bodyClass?.split(/\s+/).includes('caption-product-page') ? ProductNightLayout : BaseLayout;
---

<Layout
  title={title}
  description={description}
  lang={lang}
  canonicalPath={canonicalPath}
  alternates={alternates}
  ogImage={ogImage}
  bodyClass={bodyClass}
>
  <slot />
</Layout>
